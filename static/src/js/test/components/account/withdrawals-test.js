/**
 * TODO:
 * - renders the component
 * - mock superanget and simulate POST requests
 * - on errors from backend - display them (if form-field--error) in DOM
 * - on success update user info (new balance / bonus etc.)
 *
 */
