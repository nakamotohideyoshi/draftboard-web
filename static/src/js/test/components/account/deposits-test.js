/**
 * TODO:
 * - rendering of component
 * - quickDeposits modifying DOM
 * - rendering of modals correctly here too (prolly)
 * - checking if forms from backend are rendered in the popup forms (for new card)
 * - on removing of default paymentMethod, set new default paymentMethod that
 * is synced with backend
 */
