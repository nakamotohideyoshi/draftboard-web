/**
 * TODO:
 * - rendering of components in the section
 *
 * transactions form:
 *   - on date select, change url and browser history (pushState)
 *   - API call to fetch transactions in the selected period
 *
 * transactions table:
 *   - rendering
 *
 * details sidebar:
 *   - opening and removing from DOM on sidepane close
 *
 */
