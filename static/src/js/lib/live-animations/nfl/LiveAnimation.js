export default class LiveAnimation {
    
    /**
     * Plays the animation.
     *
     * @param {NFLPlayRecapVO}   The recap data.
     * @param {NFLField}         The field of play.
     * @return {Promise}
     */
    play (recap, field) {
        return Promise.resolve();
    }
}
