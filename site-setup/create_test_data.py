#
# creates some basic test data for an 2014 NBA game
# with contests, lineups, stats, and users so there are some demo api calls

# AMOUNT  = 5                         # number of players to get_or_create
# PLAYER  = 'player'
#
# from django.contrib.auth.models import User
#
# for p in range(AMOUNT):
#     player, created = User.objects.get_or_create(username=PLAYER)
#     player.set_password(PLAYER)
#     player.save()
